import styled from "styled-components";

const FlexCm = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
`;

export const KeyValueFlex = styled(FlexCm)`
    gap: 0.5rem;
`;

export default FlexCm;
